{
  "name": "resolutor-legal-support",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/inject-seo-structured-data-dist.mjs",
    "postbuild": "node scripts/build-sitemap.mjs",
    "sync:articles-meta": "node scripts/sync-article-source-meta.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1"
  }
}
